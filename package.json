{
  "name": "aarambham2026-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd REGISTRATION/forms && npm run build",
    "build:registration": "cd REGISTRATION/forms && npm run build"
  },
  "workspaces": [
    "REGISTRATION/forms"
  ]
}
